<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Canvas.Views.StudentCoursesView"
             Title="My Courses">

    <VerticalStackLayout Spacing="15" Padding="20">

        <Label Text="{Binding StudentName, StringFormat='Courses for {0}'}"
               FontSize="26" HorizontalOptions="Center" />

        <CollectionView ItemsSource="{Binding Courses}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Gray" Padding="15" Margin="0,5">
                        <VerticalStackLayout>
                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding Code}" FontSize="14" />
                            <Label Text="{Binding Semester}" FontSize="12" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Label Text="This student is not enrolled in any courses."
               IsVisible="{Binding HasNoCourses}"
               HorizontalOptions="Center" />

        <Button Text="Back" Clicked="OnBackClicked" />

    </VerticalStackLayout>
</ContentPage>